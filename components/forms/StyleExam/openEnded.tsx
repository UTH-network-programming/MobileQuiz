import React from "react";
import { View } from "react-native";
import TextUI from "../../ui/textUI";
import TextExam from "../textExam";

const OpenEnded = () => {
  return (
    <View>
      <TextExam text="letan đây * xin chào" />
    </View>
  );
};

export default OpenEnded;
